# 去许可证化与 Xpack 遗留清理执行任务

## 1. 任务目标

本仓库已经取消许可证限制，后续只维护一套可直接使用的自托管版本。本任务需要：

1. 删除已经失效的许可证校验、许可证页面和企业版动态加载入口。
2. 删除当前仓库确实没有源码引用的企业版依赖及兼容分支。
3. 保留并迁移仍被社区版使用的认证、多节点、告警和同步能力。
4. 最终消除会误导维护者的 `xpack`、`enterprise`、`isXpackOrEE` 等历史命名。

这里的核心原则是：**删除许可证体系，不等于直接删除所有包含 `xpack` 字样的代码。** 当前部分 `utils/xpack` 已经承载社区版实际功能，必须先迁移再删除旧目录。

## 2. 当前确认结论

### 2.1 当前仓库不存在的扩展源码

仓库中不存在以下真正的企业功能源码目录：

- `core/xpack`
- `core/enterprise`
- `agent/xpack`
- `agent/enterprise`
- `frontend/src/xpack`
- `frontend/src/enterprise`

前端通过 `import.meta.glob()` 加载上述可选目录，但当前构建实际加载不到任何模块。

### 2.2 仍在使用、禁止直接删除的能力

以下代码虽然位于 `utils/xpack` 或使用 `xpack.*` 命名，但属于当前社区版运行链路：

- `core/utils/xpack/providers`、`core/utils/xpack/helper`
  - 登录认证
  - MFA
  - Passkey
  - 当前用户信息
  - 多节点请求和同步
- `agent/utils/xpack/providers`、`agent/utils/xpack/helper`
  - 节点模式识别
  - 节点证书和主控通信
  - 告警投递
  - 多节点请求 Transport
- `xpack.MultiNodeProvider` 的调用方
  - 备份账号同步
  - 脚本库同步
  - SSL、告警及节点相关业务
- `xpack.AuthProvider` 的调用方
  - `core/app/api/v2/auth.go` 中的登录、MFA、Passkey、用户信息等接口

未经迁移，不允许直接删除这些目录或把调用改成空实现。

## 3. 执行约束

1. 按阶段执行，每个阶段独立提交；不要一次性大删除。
2. 不修改与许可证/Xpack 清理无关的业务逻辑或格式。
3. 不得降低当前社区版功能：登录、多节点、节点升级、终端、备份同步、脚本库同步、告警必须保留。
4. 不新增新的 feature flag 或许可证替代开关。
5. 如果发现私有构建流程会在构建时注入额外源码，立即停止删除依赖并记录证据；当前公开仓库和 GitHub CI 未发现此流程。
6. 所有代码变更完成后按项目规则提升补丁版本、提交、打 tag 并推送触发 stable CI。
7. 只有 `packaging/*.sh` 等发布脚本发生变化时，才需要额外验证 Cloudsmith bootstrap 同步。

## 4. 阶段一：删除许可证运行时流程

### 4.1 前端许可证守卫

处理：

- `frontend/src/routers/index.ts`
  - 删除 `enterpriseLicenseCheckWhiteList`。
  - 删除 `clearLicenseStatus()`。
  - `clearLoginStatus()` 不再操作许可证状态。
  - 删除登录后根据 `isEnterprise`、`isEnterpriseLicenseLoaded`、`isEnterpriseLicensed` 跳转许可证页的逻辑。
  - 删除 `EnterpriseLicenseRequired` 的特殊路由处理。
- 检查 `frontend/src/api/index.ts` 的 HTTP 402 拦截器。
  - 如果仍跳转 `/enterprise/license-required`，改为正常展示后端错误，不再进入许可证页面。
  - 不得吞掉后端原始错误。
- 从全局 Store、组合式 API 和类型中删除：
  - `isEnterpriseLicenseLoaded`
  - `isEnterpriseLicensed`
  - 仅为许可证页面服务的状态和 action

验证：

- 未登录仍正确跳转登录页。
- 已登录用户不会因为 edition 或 HTTP 402 被重定向到许可证页面。
- 登录、退出、入口码登录不受影响。

### 4.2 删除许可证页面和 API

删除或迁移：

- `frontend/src/views/setting/license-required/`
- `frontend/src/routers/router.ts` 中的 `EnterpriseLicenseRequired` 路由。
- `frontend/src/api/modules/setting.ts` 中仅用于以下操作的接口：
  - 获取企业许可证
  - 上传企业许可证
  - 社区版恢复状态
  - 下载或恢复社区版包
- 对应 DTO、组件、图片和只被上述页面引用的工具函数。
- 各语言文件中只与许可证导入、同步、设备 ID、恢复社区版有关的文案。

注意：删除前必须用 `rg` 逐项确认没有其他调用方；不要批量删除整个 `license` 翻译命名空间，部分告警文案可能仍引用它。

验证：

```bash
rg -n "EnterpriseLicenseRequired|isEnterpriseLicensed|isEnterpriseLicenseLoaded|uploadEnterpriseLicense|getEnterpriseLicense|license-required" frontend/src
```

预期：没有有效运行时代码引用；允许迁移说明或历史 changelog 中出现。

## 5. 阶段二：清理后端许可证和企业版死分支

### 5.1 路由和中间件

检查并删除仅服务于许可证的内容：

- `core/middleware/password_expired.go` 中企业许可证接口白名单。
- `core/middleware/demo_handle.go` 中当前仓库不存在的企业 API 条目。
- `core/constant/common.go` 中不存在路由对应的 enterprise/xpack 菜单白名单。
- 许可证错误码和多语言错误文案：
  - `ErrXpackVersion`
  - `ErrXpackNotFound`
  - `ErrXpackExceptional`
  - `ErrXpackLost`
  - `ErrXpackTimeout`

每个错误码删除前必须确认没有社区实现仍返回它。例如 agent 的某些 provider 当前仍可能返回 `ErrXpackNotFound`；应先把该返回值改为更准确的“功能未实现/不支持”，再删除文案。

### 5.2 企业数据库兼容代码

检查以下命令是否仍需兼容历史安装：

- `core/cmd/server/cmd/user.go`
- `core/cmd/server/cmd/user-info.go`
- `core/cmd/server/cmd/reset.go`
- `core/cmd/server/cmd/update.go`
- `core/init/hook/hook.go`
- `core/middleware/operation.go`

决策规则：

- 如果代码只操作 `enterprise.db` 且新版本不再生成、读取该数据库，可以删除。
- 如果它用于从旧企业安装迁移到当前统一版本，保留一个明确标注的兼容迁移周期，并写测试；不要长期保留隐式分支。
- 不允许在没有确认数据迁移影响的情况下直接删除用户重置或管理员恢复逻辑。

## 6. 阶段三：迁移仍在使用的 Xpack Provider

这一阶段是重命名和目录迁移，不改变业务行为。

### 6.1 Core

建议目标目录：

```text
core/platform/auth/
core/platform/multinode/
```

迁移映射：

- `core/utils/xpack/providers/auth.go` → `core/platform/auth/provider.go`
- `core/utils/xpack/helper/auth_helper.go` → `core/platform/auth/community.go`
- `core/utils/xpack/providers/multi_node.go` → `core/platform/multinode/provider.go`
- `core/utils/xpack/helper/multi_node_helper.go` → `core/platform/multinode/service.go`
- `xpack.AuthProvider` → 中性名称，例如 `auth.Provider`
- `xpack.MultiNodeProvider` → 中性名称，例如 `multinode.Provider`

要求：

- 保持现有接口方法签名和行为。
- 优先机械迁移，不顺便重构认证或多节点协议。
- 迁移后运行所有认证和节点相关测试。

### 6.2 Agent

建议目标目录：

```text
agent/platform/multinode/
agent/platform/alert/
```

迁移映射：

- `agent/utils/xpack/providers/multi_node.go`
- `agent/utils/xpack/helper/multi_node.go`
- `agent/utils/xpack/providers/alert.go`
- `agent/utils/xpack/helper/alert.go`
- `agent/utils/xpack/alert_delivery.go`

必须重点验证：

- `/etc/3panel/node.json` 读取和节点模式判断。
- mTLS 证书校验。
- 主控与 agent 请求 Transport。
- 节点升级、健康检查和版本上报。
- 告警任务和自定义 Webhook。

### 6.3 删除旧目录和构建标签

Provider 调用全部迁移后：

- 删除 `core/utils/xpack/`。
- 删除 `agent/utils/xpack/`。
- 评估并删除仅为外部企业源码覆盖准备的构建标签：
  - `//go:build !xpack && !enterprise`
  - `core/router/entry.go`
  - `agent/router/entry.go`
  - `core/server/init.go`
  - `agent/server/init.go`
- 将这些文件改成普通社区版入口，保证不带 tag 的默认构建行为不变。

验证：

```bash
rg -n "utils/xpack|xpack\.AuthProvider|xpack\.MultiNodeProvider|go:build.*(xpack|enterprise)" core agent
```

预期：没有运行时代码引用。

## 7. 阶段四：清理前端 Xpack/Enterprise 命名

### 7.1 动态扩展加载器

检查并移除当前永远加载不到模块的逻辑：

- `frontend/src/extensions/optional.ts`
- `frontend/src/extensions/routes.ts`
- `frontend/src/extensions/theme.ts`
- `frontend/src/extensions/xpack.ts`
- `frontend/src/extensions/footer-setting.ts`

处理方式：

- 如果调用方只需要社区默认值，直接返回明确的社区实现。
- 如果文件已没有调用方，删除文件和导入。
- 不允许保留静默的 `null` 动态加载作为永久兼容层。

### 7.2 Store 与工具函数

处理：

- 将 `isXpackOrEE` 改成真实业务语义；如果所有功能均开放，直接删除条件判断。
- 将 `loadProductProFromDB`、`getXpackSetting` 等改为中性名称，或在无实际作用时删除。
- 删除 `IsEnterprise`、`IsFxplay` 判断前，逐项确认它们是否仍影响国际版、下载源或 UI；不要根据名字直接删除。

### 7.3 翻译键和路由

按功能迁移翻译命名空间：

- `xpack.node.*` → `node.*` 或 `multiNode.*`
- `xpack.alert.*` → `alert.*`
- `xpack.user.*` → `user.*`/`auth.*`
- `xpack.waf.*` → `waf.*`

历史路径 `/xpack/...` 的处理规则：

- 当前实际存在并仍受支持的功能迁移到中性路径。
- 已不存在的功能删除菜单、白名单和跳转。
- 如需兼容用户浏览器中的旧缓存路径，可保留一个版本周期的显式 redirect；不得让兜底 404 被误判为有效路由。

## 8. 阶段五：依赖清理

Provider 和许可证代码清理完成后，在两个 Go 模块分别执行：

```bash
cd core
go mod tidy
go test ./...

cd ../agent
go mod tidy
go test ./...
```

重点审查 `core/go.mod` 删除的企业残留依赖，例如：

- OIDC/SAML/LDAP 相关包
- 企业报表/PDF 相关包
- 仅由已删除企业源码使用的 OAuth 和 XML 签名包

不要只因为 `go mod tidy` 成功就认定安全；还要确认：

- 默认构建通过。
- GitHub stable 打包脚本通过。
- 不再存在需要这些依赖的构建标签源码。
- 没有外部源码注入或私有 overlay 构建流程。

前端依赖通过项目实际使用的包管理器更新锁文件，不手工编辑 `package-lock.json`。

## 9. 每阶段验证要求

### 9.1 静态检查

```bash
git diff --check
rg -n "license-required|EnterpriseLicenseRequired|isEnterpriseLicensed|isEnterpriseLicenseLoaded" frontend/src core agent
rg -n "utils/xpack|@/xpack|@/enterprise|isXpackOrEE" frontend/src core agent
```

对剩余匹配项逐条解释，不能仅报告匹配数量。

### 9.2 后端测试

```bash
cd agent && go test ./...
cd ../core && go test ./...
```

若 core 测试依赖 `core/cmd/server/web/assets`，先按 CI 的正式前端构建流程生成 assets，不得用空文件绕过 `embed`。

### 9.3 前端检查

```bash
cd frontend
npm ci
npm run type-check
npm run build:pro
```

如果本机 Node/npm 环境不可用，必须明确报告；不能用 ESLint 通过替代生产构建结论。

### 9.4 功能回归

至少验证：

1. 本地管理员登录、退出、修改密码。
2. MFA 和 Passkey（如当前版本支持）。
3. 主节点与节点切换。
4. 多机管理健康检查和节点版本刷新。
5. 节点升级并自动重启。
6. 终端连接。
7. 备份账号和脚本库增删改。
8. 告警与自定义 Webhook。
9. 页面不再出现许可证入口、许可证重定向或 402 许可证兜底页。

## 10. 提交拆分建议

建议提交顺序：

1. `refactor(license): remove frontend license flow`
2. `refactor(license): remove backend license remnants`
3. `refactor(core): move auth and multi-node providers out of xpack`
4. `refactor(agent): move multi-node and alert providers out of xpack`
5. `refactor(frontend): remove xpack extension loaders and rename feature keys`
6. `chore(deps): remove unused enterprise dependencies`
7. `chore(release): publish next patch version`

每个提交只包含对应阶段文件，禁止把格式化整个语言文件、生成文档乱序或无关依赖升级混入提交。

## 11. 完成标准

只有同时满足以下条件，任务才能标记完成：

- 项目不再包含许可证导入、验证、同步、恢复社区版的运行时流程。
- 不再因 edition 或 HTTP 402 跳转许可证页面。
- `core/utils/xpack` 和 `agent/utils/xpack` 的社区能力已经迁移，旧目录可删除。
- 前端不再动态加载不存在的 xpack/enterprise 模块。
- 社区功能不再依赖 `isXpackOrEE` 才显示或执行。
- 默认构建不再包含 `xpack`/`enterprise` 构建标签兼容入口。
- 不再保留无源码引用的企业依赖。
- agent/core 全量测试通过。
- 前端类型检查与生产构建通过，或已明确记录与本次改动无关的既有基线错误。
- 登录、多节点、节点升级、终端、备份、脚本库和告警经过实际回归。
- 已提升补丁版本并推送 tag，stable CI 成功。

## 12. 明确禁止事项

- 禁止直接执行 `rm -rf core/utils/xpack agent/utils/xpack`。
- 禁止为了消除编译错误把认证、多节点或告警 provider 改成永久空实现。
- 禁止一次性批量替换所有 `xpack` 字符串而不判断语义。
- 禁止删除旧数据库兼容逻辑而不确认升级用户的数据迁移影响。
- 禁止把“默认构建通过”等同于“带标签、发布打包和运行时都通过”。
- 禁止在没有前端生产构建证据时合并大规模路由或 Store 清理。
