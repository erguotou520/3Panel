{
    "name": "cf-appstore-sync",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "tail": "wrangler tail",
        "sync": "curl -s 'http://127.0.0.1:8787/sync?force=true'",
        "sync-resource": "curl -s 'http://127.0.0.1:8787/sync-resource?dry=true'",
        "status": "curl -s 'http://127.0.0.1:8787/status'"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
